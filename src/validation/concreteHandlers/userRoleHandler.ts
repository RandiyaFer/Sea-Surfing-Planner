// import { User } from '../../model/entities/user-entity';
// import { BaseValidationHandler } from '../baseHandler/baseValidationHandler';

// export class UserRoleHandler extends BaseValidationHandler<User> {
//   // async handle(user: User): Promise<string | null> {
//   //   if (!user.userRole || !user.userRole.roleName || user.userRole.roleName.trim() === '') return 'User role is required';
//   //   return null;
//   // }
// }
